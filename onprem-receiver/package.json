{
  "name": "watapp-onprem-receiver",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only Baileys listener that forwards inbound WhatsApp to the CRM /api/ingest. Runs on the on-prem residential box (where Baileys connects); never sends.",
  "main": "index.mjs",
  "scripts": { "start": "node index.mjs" },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.9",
    "pino": "^9.5.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
