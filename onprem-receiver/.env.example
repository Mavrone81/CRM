# Copy to .env and fill in. INGEST_TOKEN MUST match the CRM server's INGEST_TOKEN.
INGEST_TOKEN=change-me-to-a-long-random-string
CRM_INGEST_URL=https://crm.urbanwerkzsg.com/api/proxy/ingest
# The CRM number id this WhatsApp line maps to (used for sticky assignment + thread label).
NUMBER_ID=onprem-n2
