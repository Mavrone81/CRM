node_modules/
.next/

# WhatsApp link session — host-specific, never commit
server/sessions/

# Live CRM data + runtime config — server-managed, bind-mounted
server/data/leads.json
server/data/config.json

# Secrets
.env
.env.local
*.log

# Uploaded documents (agreements etc.) — server-managed, bind-mounted, never commit
server/data/documents/
server/data/documents.json
